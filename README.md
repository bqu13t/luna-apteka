## Getting Started

First, run the development server:

```bash
npm install
npm run deploy:dev
```

To start prod server:

```bash
npm install
npm run deploy:prod
```

After that, the app is available at

```bash
http://localhost:3000
```
